function subscribe(e){e.preventDefault();const email=e.target.querySelector('input').value;alert(`Thanks — ${email} is on the list. Connect this form to your email provider before launch.`);e.target.reset();}